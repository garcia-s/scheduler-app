// Self explanatory
Service
	name
	per_user_limitation //Parallel limitations for the service per qualified user
	designated_areas //Space limitations for the service
	is_time_sensitive: //If is time sensitive it needs a specific timeframe to be executed that cannot be designated by the scheduling user
	timeframe //If needed this shoul give a duration for the specified service  

// "Geographical" organizational structure of ServiceSpaces.
ServiceSpaceArea
	name
	spaces // ServiceSpace(s)
	
ServicesSpace
	name
	//Dont know what else

Users
	name
	qualified_services: //Service(s) that can be executed by the user
	role //Admin or normal user.
	
