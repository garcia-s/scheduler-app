import 'package:flutter/material.dart';

class RecoveryRoute extends StatelessWidget {
  static const String address = '/recovery';
  const RecoveryRoute({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}