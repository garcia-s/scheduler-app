import 'package:flutter/material.dart';

class UpdateTableScreen extends StatelessWidget {
  const UpdateTableScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
