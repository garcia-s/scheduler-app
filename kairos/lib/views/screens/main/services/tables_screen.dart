import 'package:flutter/material.dart';

class TableScreen extends StatelessWidget {
  final int _permissions;
  const TableScreen(this._permissions, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return Container();
  }
}
